I have recorded in my backlog a note that on Windows, the 'esc' script installed to the path is not executable by Python, so you can't actually start it by typing 'esc'. I don't immediately have a Windows computer available to test this, but can you check to see if there's something misconfigured that we haven't addressed during the current round of changes and modernization that would still cause this? It would have been probably as of the 1.0.0 release that this was happening
