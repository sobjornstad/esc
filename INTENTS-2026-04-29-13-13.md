Currently in the help screen, if you are trying to apply incommensurable units, you get a generic "An error would occur," as shown in the screenshot. Let's add special handling in the speculative execution in the help that detects unit errors and displays an appropriate message for them instead of this generic one.

---

Let's tweak the formatting slightly. Currently it renders like

If you executed this command now...                           │
│                                     ││    An error would occur. (Incommensurable units: dogs vs c...│

It should instead look like

If you executed this command now...
    A unit error would occur:
    Incommensurable units: dogs vs cats

That is, the error text should say "a unit error would occur", and the rest should be on a new line

If "an error would occur" is hard-coded somewhere else and hard to override, let's just do the second part of putting it on a new line not in parentheses
